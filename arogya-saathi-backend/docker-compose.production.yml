version: '3.8'
services:
  app:
    build: .
    ports:
      - \"3000:3000\"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/prod.db
      - JWT_SECRET=your_very_secure_jwt_secret_here
    volumes:
      - app_data:/app
    restart: unless-stopped

volumes:
  app_data:
